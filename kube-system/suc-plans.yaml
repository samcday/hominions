apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: suc-plans
  namespace: kube-system
spec:
  chart:
    spec:
      chart: kube-system/suc-plans
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: home-cluster
        namespace: flux-system
  install:
    remediation:
      retries: -1
  interval: 1h
  releaseName: suc-plans
  upgrade:
    remediation:
      retries: -1
  valuesFrom:
    - kind: ConfigMap
      name: k3s-config
      valuesKey: k3s-server.yaml
      targetPath: server
    - kind: Secret
      name: cluster-backups-bucket
      valuesKey: etcd-s3-access-key
      targetPath: server.etcd-s3-access-key
    - kind: Secret
      name: cluster-backups-bucket
      valuesKey: etcd-etcd-s3-secret-key
      targetPath: server.etcd-etcd-s3-secret-key
    - kind: ConfigMap
      name: k3s-config
      valuesKey: k3s-agent.yaml
      targetPath: agent
